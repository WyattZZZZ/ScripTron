{
  "name": "example-skill",
  "kind": "skill",
  "description": "Example TronHub skill that declares required CLIs and optional model providers.",
  "version": "0.1.0",
  "requires": {
    "clis": [
      {
        "name": "example-tool-cli",
        "version": ">=0.1.0",
        "required": true,
        "reason": "Runs the local operation used by this skill."
      }
    ],
    "models": [
      {
        "name": "example-model-provider",
        "version": ">=0.1.0",
        "required": false,
        "reason": "Can improve generation quality when the user asks for natural-language output."
      }
    ]
  },
  "capabilities": [
    "read project files",
    "call required CLIs",
    "return Markdown responses"
  ]
}
