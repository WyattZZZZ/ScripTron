# Example Skill

This package demonstrates a TronHub skill package.

Skills are instruction bundles. They should declare required CLIs and optional model providers in `skill.json`, then describe their workflow in `SKILL.md`.
