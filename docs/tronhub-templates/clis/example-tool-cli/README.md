# Example Tool CLI

This package demonstrates a normal TronHub CLI/tool provider.

Replace `example-tool` in `install.sh` and `run.sh` with the real executable. Keep `manifest.json.kind` set to `tool` so the package appears in CLI Market and CLI Management.
