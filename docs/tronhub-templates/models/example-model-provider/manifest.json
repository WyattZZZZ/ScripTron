{
  "name": "example-model-provider",
  "kind": "model",
  "description": "Example TronHub model provider. Models are CLI-backed providers with a model kind.",
  "version": "0.1.0",
  "command": "./run.sh",
  "args_schema": [
    {
      "name": "action",
      "description": "Action to run: login, chat, or version.",
      "required": true,
      "type": "string"
    },
    {
      "name": "prompt",
      "description": "Prompt text for chat actions.",
      "required": false,
      "type": "string"
    },
    {
      "name": "project_path",
      "description": "Optional project path for workspace-aware prompts.",
      "required": false,
      "type": "string"
    }
  ],
  "examples": [
    "./run.sh --action login",
    "./run.sh --action chat --prompt \"Summarize this project\""
  ]
}
