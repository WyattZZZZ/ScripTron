# Example Model Provider

This package demonstrates a model provider as a special CLI.

Replace `example-model` in `install.sh` and `run.sh` with the real provider executable. Keep `manifest.json.kind` set to `model` so the package appears in Model Management instead of CLI Management.
